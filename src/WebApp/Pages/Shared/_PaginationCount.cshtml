@model GaEpd.AppLibrary.Pagination.IPaginatedResult
Total:
@Html.DisplayFor(m => Model.TotalCount)
@if (Model.TotalPages > 1)
{
    <br />
    @($"Showing: {Model.FirstItemIndex} – {Model.LastItemIndex}")
}
