@page "{id:Guid?}"
@model EditRolesModel
@{
    ViewData["Title"] = "Edit User Roles";
}

<h1>@ViewData["Title"]</h1>
<hr />

<h2>@Model.DisplayStaff.DisplayName</h2>

<dl class="row">
    <dt class="col-sm-3">Email</dt>
    <dd class="col-sm-9">@Model.DisplayStaff.Email</dd>

    <dt class="col-sm-3">Phone</dt>
    <dd class="col-sm-9">@Html.DisplayFor(m => m.DisplayStaff.Phone, "StringOrNone")</dd>

    <dt class="col-sm-3">Office</dt>
    <dd class="col-sm-9">@Html.DisplayFor(m => m.OfficeName, "StringOrNone")</dd>
</dl>

<div class="p-3 border rounded-3 bg-light mb-3">
    <form method="post">
        <div asp-validation-summary="All" class="alert alert-danger" role="alert">
            Roles update failed:
        </div>

        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-3">
            @for (var i = 0; i < Model.RoleSettings.Count; i++)
            {
                var index = i;
                <div class="col">
                    <div class="card h-100">
                        <label class="form-check-label p-1 h-100" asp-for="@Model.RoleSettings[index].IsSelected">
                            <input class="form-check-input m-2" type="checkbox" asp-for="@Model.RoleSettings[index].IsSelected" aria-describedby="role-help-block-@index" />
                            <p class="d-inline-block w-75">
                                @Model.RoleSettings[index].DisplayName<br />
                                <small id="role-help-block-@index" class="card-text">@Model.RoleSettings[index].Description</small>
                                <input type="hidden" asp-for="@Model.RoleSettings[index].Name" />
                            </p>
                        </label>
                    </div>
                </div>
            }
        </div>

        <div class="mt-3">
            <input asp-for="UserId" type="hidden" />
            <button type="submit" class="btn btn-primary col-sm-3 me-2">Save Roles</button>
            <a asp-page="Details" asp-route-id="@Model.UserId" class="btn btn-outline-secondary col-md-2">Cancel</a>
        </div>
    </form>
</div>
